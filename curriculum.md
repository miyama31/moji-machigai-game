# 初心者向け Claude Code セミナー カリキュラム

**対象**: プログラミング経験があるがAIコーディングツールは初めての方  
**所要時間**: 約3時間（休憩込み）  
**使用リポジトリ**: 文字の間違い探しゲーム（`index.html`）

---

## 全体の流れ

| # | モジュール | 内容 | 時間 |
|---|-----------|------|------|
| 1 | Claude Code とは | 概要・できること | 20分 |
| 2 | セットアップ | インストール・初期設定 | 20分 |
| 3 | 基本操作 | コマンドの使い方 | 30分 |
| ☕ | 休憩 | | 10分 |
| 4 | コードを読む | ファイル理解・質問の仕方 | 30分 |
| 5 | コードを変える | 機能追加・バグ修正 | 40分 |
| 6 | Git 連携 | コミット・プッシュ | 20分 |
| 7 | まとめ・Q&A | ベストプラクティス | 10分 |

---

## モジュール 1 — Claude Code とは（20分）

### 学習目標
- Claude Code が「何を自動化してくれるのか」を自分の言葉で説明できる
- 通常のチャット型AI（Claude.ai）との違いを理解する

### 講義内容

#### 1-1. 位置づけ
- **Claude.ai**：ブラウザで使うチャットAI。コードを貼り付けて相談できる
- **Claude Code**：ターミナルで動くCLIツール。**ファイルを直接読み書きし、コマンドを実行**できる
- Claude Code は「コードを知っているペアプログラマー」がターミナルに常駐しているイメージ

#### 1-2. できること（デモ）
| 操作 | 具体例 |
|------|--------|
| ファイルを読む | `index.html` の仕様を説明してもらう |
| コードを書く | 新しい機能のコードを自動生成してもらう |
| バグを直す | エラーメッセージを渡して原因を特定してもらう |
| git を操作する | コミットメッセージを自動生成してコミット |
| テストを実行する | `npm test` を走らせて結果を解釈してもらう |

#### 1-3. 注意点
- ファイルの**変更は元に戻せない**場合がある → git で管理することが重要
- AI は間違えることがある → 変更内容は必ず自分でレビューする
- 機密情報（APIキー、パスワード）はプロンプトに含めない

### ハンズオン
```
質問: 「Claude Code を一言で説明するとしたら？」→ グループで共有
```

---

## モジュール 2 — セットアップ（20分）

### 学習目標
- Claude Code をインストールし、プロジェクトで起動できる

### 手順

#### 2-1. 前提条件の確認
```bash
node --version   # v18 以上
git --version    # 2.x 以上
```

#### 2-2. インストール
```bash
npm install -g @anthropic-ai/claude-code
```

#### 2-3. 認証
```bash
claude
# ブラウザが開いてAnthropicアカウントでログイン
# または ANTHROPIC_API_KEY を設定
```

#### 2-4. プロジェクトを開く
```bash
git clone https://github.com/miyama31/moji-machigai-game.git
cd moji-machigai-game
claude   # このディレクトリで Claude Code を起動
```

#### 2-5. 起動確認
Claude Code が起動したら以下を入力：
```
このプロジェクトにはどんなファイルがありますか？
```

### よくあるトラブル
| 症状 | 対処 |
|------|------|
| `command not found: claude` | `npm install -g` のパスを確認、`export PATH` を設定 |
| 認証エラー | `claude logout` → `claude` で再ログイン |
| 日本語が文字化け | ターミナルの文字コードを UTF-8 に設定 |

---

## モジュール 3 — 基本操作（30分）

### 学習目標
- 質問・コマンド実行・ファイル操作の基本フローを理解する
- `/help` などの組み込みコマンドを使える

### 3-1. インタラクティブモードの基本

Claude Code は起動後、**対話型のプロンプト**になる。

```
> （ここに日本語でも英語でも自由に入力）
```

**便利なキーボードショートカット**
| キー | 動作 |
|------|------|
| `↑` / `↓` | 履歴を移動 |
| `Ctrl+C` | 現在の処理をキャンセル |
| `Ctrl+D` | Claude Code を終了 |
| `Shift+Enter` | 改行（複数行入力） |

### 3-2. 組み込みスラッシュコマンド

```bash
/help          # 使い方一覧を表示
/clear         # 会話履歴をリセット（コンテキストをクリア）
/compact       # 長い会話を要約して圧縮
/status        # 現在の状態確認
```

### 3-3. 良いプロンプトの書き方

**悪い例**（あいまいすぎる）
```
コードを直して
```

**良い例**（具体的・背景・期待結果を含む）
```
index.html の onCellClick 関数で、
間違いをクリックしたときのペナルティを「-2秒」から「-3秒」に変更してください。
変更箇所だけ教えてください。
```

**質問の型（テンプレート）**
```
[何を] [どうしたい] [理由/制約があれば]
例: スコア計算のロジックを、残り時間だけでなくステージ数も考慮するように変更したい。
```

### ハンズオン（10分）
以下をClaude Codeに聞いてみよう：
1. `index.html` のゲームのルールを日本語で説明してもらう
2. `getTimeByStage` 関数が何をしているか解説してもらう
3. 現在のスコア計算式を確認してもらう

---

## ☕ 休憩（10分）

---

## モジュール 4 — コードを読む（30分）

### 学習目標
- Claude Code にコードベースを探索させ、仕様を正確に把握できる
- 「なぜこのコードがこう動くか」を説明してもらえる

### 4-1. ファイル全体の把握

```
このゲームのアーキテクチャを説明してください。
状態管理、レンダリング、イベント処理の観点から整理してください。
```

### 4-2. 特定の処理を追いかける

```
プレイヤーが正しいセルをクリックしてから「次のステージへ」ボタンが
有効になるまでの処理の流れをステップごとに説明してください。
```

### 4-3. 潜在的な問題を見つけてもらう

```
このコードにバグや改善点はありますか？
セキュリティ、パフォーマンス、ユーザー体験の観点から教えてください。
```

### 4-4. テスト観点を洗い出す

```
このゲームを手動でテストするとしたら、
どんなケースを確認すべきか一覧を作ってください。
```

### ハンズオン（15分）

**演習**: `pickPair` 関数を理解する
1. 関数の動作をClaude Codeに説明してもらう
2. 「ひらがな同士でも同じ行の文字（例: あ と い）が出ることはあるか？」と聞く
3. その答えを自分でコードを読んで確認する

---

## モジュール 5 — コードを変える（40分）

### 学習目標
- 機能追加・バグ修正のワークフローを実践できる
- Claude Code の提案を採用・却下する判断ができる

### 5-1. 小さな変更から始める（10分）

**演習 A: ペナルティ秒数を変更する**

```
index.html の onCellClick 関数で、
間違いをクリックしたときのペナルティを -2秒 から -3秒 に変えてください。
```

Claude Code が変更を提案したら：
- 変更箇所を自分でも確認する
- 意図通りか？副作用はないか？を考える
- ブラウザで動作確認する

### 5-2. 新機能を追加する（15分）

**演習 B: ハイスコア表示を追加する**

```
ゲームにハイスコア機能を追加してください。
要件:
- localStorage にベストスコアを保存する
- ゲーム画面のバッジ一覧（.info クラス）にハイスコアを表示する
- 新記録を出したときはメッセージで祝福する
- 「最初から」ボタンを押してもハイスコアはリセットされない
```

### 5-3. バグを修正する（15分）

**演習 C: タイマーのバグを調査・修正する**

以下の状況を作って再現確認する：
- 「次のステージへ」を押した直後に「最初から」を押す

```
「次のステージへ」を押した直後に「最初から」を押すと
タイマーが2重に動くことがあります。
state と clearInterval の使い方を確認して原因と修正方法を教えてください。
```

### 変更レビューのチェックリスト
変更を受け入れる前に以下を確認：
- [ ] 変更箇所を自分で読んで理解できた
- [ ] ブラウザで実際に動かして確認した
- [ ] 他の機能を壊していない
- [ ] コードスタイルが既存のものと揃っている

---

## モジュール 6 — Git 連携（20分）

### 学習目標
- Claude Code と git を連携させてコミット・プッシュできる
- コミットメッセージの自動生成を活用できる

### 6-1. 変更状態の確認

```
今の変更内容をまとめて説明してください。
```

または直接 git コマンドを聞く：

```
git diff の結果を解説してください
```

### 6-2. コミットを依頼する

```
今の変更を git にコミットしてください。
コミットメッセージは日本語で書いてください。
```

Claude Code は自動的に：
1. `git diff` で変更内容を確認
2. 適切なコミットメッセージを生成
3. `git add` → `git commit` を実行

### 6-3. ブランチ操作

```
feature/high-score という名前の新しいブランチを作って
そこで作業するようにしてください。
```

### 6-4. よくある git フロー

```bash
# Claude Code に依頼する例
「main ブランチに変更がないか確認して、あれば pull してください」
「今の変更を feature/xxx ブランチにコミットして push してください」
「コミット履歴を見やすく表示してください」
```

### ハンズオン（10分）

演習 B または C で加えた変更を：
1. 内容をClaude Codeに説明してもらう
2. 日本語のコミットメッセージを提案してもらう
3. コミットを実行してもらう

---

## モジュール 7 — まとめ・Q&A（10分）

### 今日学んだこと

| スキル | ポイント |
|--------|---------|
| 基本操作 | インタラクティブモード、スラッシュコマンド |
| コードを読む | 仕様把握、バグ発見、テスト観点の洗い出し |
| コードを変える | 機能追加・修正のワークフロー |
| Git 連携 | コミット・プッシュの自動化 |

### ベストプラクティス

**Do**
- 具体的なプロンプトで依頼する（何を・どうしたい・制約）
- 変更前に git commit で保存しておく
- 提案された変更は必ず自分でレビューする
- 小さなステップで進める（一度に大きな変更をしない）
- CLAUDE.md にプロジェクト固有のルールを書いておく

**Don't**
- AIの提案を無条件に信頼しない
- APIキーや機密情報をプロンプトに含めない
- レビューせずにそのまま本番反映しない
- コンテキストが長くなりすぎたまま作業しない（`/compact` や `/clear` を活用）

### CLAUDE.md の活用

プロジェクトルートに `CLAUDE.md` を置くと、Claude Code が毎回読み込む：

```markdown
# プロジェクトルール

## コーディングスタイル
- インデントは2スペース
- 変数名は camelCase
- コメントは日本語で書く

## コマンド
- 動作確認: ブラウザで index.html を直接開く
- lint: なし（バニラJSのため）

## 注意事項
- localStorage のキー名は変更しない（後方互換性）
```

### 次のステップ

- **公式ドキュメント**: `claude.ai/code` でドキュメントを確認
- **CLAUDE.md の整備**: チームのルールをファイルに落とし込む
- **MCP (Model Context Protocol)**: 外部ツール（GitHub, DB など）との連携を試す
- **Hooks**: ファイル保存時・コミット時に自動アクションを設定する

---

## 付録 A — よく使うプロンプト集

### コードを理解する
```
このファイルの責務を100字以内で説明してください。
〇〇関数の引数と戻り値の型を教えてください。
このコードで一番複雑な部分はどこですか？
```

### 変更を依頼する
```
〇〇という機能を追加してください。既存の処理は変えないでください。
〇〇の行だけ変更してください。それ以外は触らないでください。
リファクタリングではなく、最小限の変更でバグを直してください。
```

### デバッグする
```
エラーメッセージ: 「〇〇」。この原因と修正方法を教えてください。
〇〇の条件で再現するバグがあります。コードのどこが怪しいですか？
```

### レビューしてもらう
```
この変更でパフォーマンス上の問題はありますか？
セキュリティ上のリスクはありますか？
このコードで考慮できていないエッジケースはありますか？
```

---

## 付録 B — セミナー後の課題

### 課題 1（基本）
`index.html` に「ヒント」ボタンを追加してください。
- ボタンを押すと残り時間が5秒減る
- 代わりに間違いの文字を0.5秒だけ赤く点滅させる

### 課題 2（応用）
ゲームに「難易度選択」を追加してください。
- かんたん：グリッドが小さく、制限時間が長い
- ふつう：現在の設定
- むずかしい：グリッドが大きく、制限時間が短い、ひらがな同士など判別が難しい文字も出る

### 課題 3（発展）
`CLAUDE.md` を作成し、このプロジェクトのルールを整備してください。
その後、課題 1 または 2 を Claude Code に依頼して、
`CLAUDE.md` があることで指示の精度が上がるかを体験してください。
